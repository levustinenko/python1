import random

random_numbers = [random.randint(1, 100) for _ in range(10)]

print("Исходный список:", random_numbers)

reversed_numbers = list(reversed(random_numbers))

print("Перевернутый список:", reversed_numbers)
